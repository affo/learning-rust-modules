This module exemplifies a case in which a function is imported from two different paths.

`one` imports `cheers` using the full path.  
`two` uses the short path that the crate provides via `pub use`.
